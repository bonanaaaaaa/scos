name: PlanetScale bootstrap

# Manual tool for the PlanetScale Postgres database (billed through the
# Cloudflare account). The deploy (deploy.yml) runs the same script first in
# every deployment and creates whatever is missing, roles included (ADR 0005
# amendment). Roles are created and rotated only there, because their
# one-time passwords go straight into the Terraform state
# (docs/deployment-pipeline.md#role-credentials-in-the-state). This workflow
# does two things: a dry run (reports the database and both roles) and a
# database-only repair (creates the database if missing, no roles). It never
# deletes anything and never resets a password. The job is bound to the prod
# environment for its secrets (and any deployment-branch rule added later);
# prod has no required reviewers (the user's
# decision), so dispatching this workflow on main is the authorization to
# run it. See docs/planetscale-bootstrap.md.

on:
  workflow_dispatch:
    inputs:
      create_database:
        description: Create the database if it is missing. This starts billing through the Cloudflare account.
        type: boolean
        default: false
      dry_run:
        description: Only report what would be created (the database and both roles).
        type: boolean
        default: false

permissions:
  contents: read

jobs:
  preflight:
    name: Check ref and running deploys
    runs-on: ubuntu-24.04
    timeout-minutes: 3
    permissions:
      actions: read
    steps:
      - name: Require the main branch
        # A run dispatched from another branch would execute that branch's
        # bootstrap script with prod secrets.
        env:
          REF: ${{ github.ref }}
        run: |
          if [ "${REF}" != "refs/heads/main" ]; then
            echo "::error::The bootstrap runs only from main, not ${REF}."
            exit 1
          fi
      - name: Refuse while a deploy is active
        # The deploy job runs this same bootstrap, and it and this job share
        # the planetscale-prod group. GitHub keeps one pending job per
        # group, so entering it could cancel a deploy job waiting there. This
        # job holds no group, so it checks before the next job claims it.
        # Runs of the shared deploy.yml are recorded under its caller.
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
        run: |
          for status in requested queued pending waiting in_progress; do
            if ! count="$(gh api "repos/${REPOSITORY}/actions/workflows/deploy-prod.yml/runs?status=${status}&per_page=1" --jq '.total_count')"; then
              echo "::error::Could not read Deploy Prod workflow runs (deploy-prod.yml)."
              exit 1
            fi
            if [ "${count}" -gt 0 ]; then
              echo "::error::A Deploy Prod run is ${status}. Wait for it to finish, then dispatch this workflow again."
              exit 1
            fi
          done

  bootstrap:
    name: Bootstrap PlanetScale
    needs: preflight
    if: ${{ github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    environment: prod
    permissions:
      contents: read
    # Shared with the deploy job and the seed: never two bootstraps, or a
    # bootstrap and a migration, at once.
    concurrency:
      group: planetscale-prod
      cancel-in-progress: false
    env:
      PSCALE_VERSION: 0.337.0
      # sha256 of pscale_0.337.0_linux_amd64.tar.gz, from the release's
      # pscale_0.337.0_checksums.txt and the GitHub release asset digest.
      PSCALE_SHA256: 1c6055dc145c7e1a16c2fc00ee66951359f9771681ffdff09ba47cd18d2acb4c
    steps:
      - name: Check PlanetScale organization
        env:
          PLANETSCALE_ORG: ${{ vars.PLANETSCALE_ORG }}
        run: |
          if [ -z "${PLANETSCALE_ORG}" ]; then
            echo "::error::The PLANETSCALE_ORG repository variable is not set."
            exit 1
          fi
      - name: Check out repository
        uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false
      - name: Install pscale
        run: |
          archive="${RUNNER_TEMP}/pscale.tar.gz"
          curl --fail --silent --show-error --location --output "${archive}" \
            "https://github.com/planetscale/cli/releases/download/v${PSCALE_VERSION}/pscale_${PSCALE_VERSION}_linux_amd64.tar.gz"
          echo "${PSCALE_SHA256}  ${archive}" | sha256sum --check --strict
          mkdir -p "${RUNNER_TEMP}/pscale-bin"
          tar -xzf "${archive}" -C "${RUNNER_TEMP}/pscale-bin" pscale
          echo "${RUNNER_TEMP}/pscale-bin" >> "${GITHUB_PATH}"
      # The lockfile-pinned Wrangler (@scos/api) mints the billing signature.
      # These setup steps have no secrets in their environment. That does not
      # isolate the later bootstrap step (an install script could still write
      # $GITHUB_ENV, $GITHUB_PATH or node_modules/.bin); the controls are the
      # frozen lockfile and pnpm-workspace.yaml's allowBuilds allow-list.
      - name: Install pnpm
        if: ${{ inputs.create_database && !inputs.dry_run }}
        uses: pnpm/action-setup@v4
        with:
          version: 12.4.2
          run_install: false
      - name: Install Node.js
        if: ${{ inputs.create_database && !inputs.dry_run }}
        uses: actions/setup-node@v6
        with:
          node-version: 24.15.0
          cache: pnpm
      - name: Install frozen dependencies
        if: ${{ inputs.create_database && !inputs.dry_run }}
        run: pnpm install --frozen-lockfile
      - name: Run bootstrap
        env:
          PLANETSCALE_ORG: ${{ vars.PLANETSCALE_ORG }}
          PLANETSCALE_SERVICE_TOKEN_ID: ${{ vars.PLANETSCALE_SERVICE_TOKEN_ID }}
          PLANETSCALE_SERVICE_TOKEN: ${{ secrets.PLANETSCALE_SERVICE_TOKEN }}
          PLANETSCALE_DATABASE: ${{ vars.PLANETSCALE_DATABASE || 'scos' }}
          PLANETSCALE_BRANCH: ${{ vars.PLANETSCALE_BRANCH || 'main' }}
          PLANETSCALE_REGION: ${{ vars.PLANETSCALE_REGION || 'ap-southeast' }}
          PLANETSCALE_CLUSTER_SIZE: ${{ vars.PLANETSCALE_CLUSTER_SIZE || 'PS-5' }}
          MIGRATION_ROLE_NAME: ${{ vars.MIGRATION_ROLE_NAME || 'scos_migrator' }}
          CREATE_DATABASE: ${{ inputs.create_database }}
          # Roles are only reported (dry run); creating or rotating them is the
          # deploy's job.
          MANAGE_ROLES: ${{ inputs.dry_run }}
          DRY_RUN: ${{ inputs.dry_run }}
          CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
          # Only the database-creating run sees the Cloudflare token; it is
          # used for `wrangler hyperdrive planetscale signature` only.
          CLOUDFLARE_API_TOKEN: ${{ inputs.create_database && !inputs.dry_run && secrets.CLOUDFLARE_API_TOKEN || '' }}
        run: infra/planetscale/bootstrap.sh
