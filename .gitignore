# Local issue-loop memory and evidence
.loop/

# Local agent skills
.claude/skills/
.claude/agents/
.agents/skills/
.codex/agents/
.env
.turbo
coverage
dist
node_modules

# Generated Prisma client (rebuilt by the turbo generate task)
packages/persistence/src/generated/

# Wrangler (apps/api Worker): local state and local secrets
.wrangler
.dev.vars
.dev.vars.*
!.dev.vars.example

# Terraform (infra/): provider caches, state, plans and crash logs are never
# committed; state and plans hold secrets. .terraform.lock.hcl stays tracked.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log

# Playwright reports and artifacts (apps/api-acceptance)
test-results/
playwright-report/
