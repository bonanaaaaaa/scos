{
  "name": "scos",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "api:dev": "pnpm --filter @scos/api dev",
    "api:start": "pnpm --filter @scos/api start",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "oxlint --deny-warnings scripts && turbo run lint",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "test": "node --test scripts/*.test.mjs && turbo run test",
    "test:integration": "turbo run test:integration",
    "validate:pr-title": "node scripts/validate-pr-title.mjs",
    "check": "pnpm build && pnpm typecheck && pnpm lint && pnpm format:check && pnpm test"
  },
  "devDependencies": {
    "@types/node": "24.13.5",
    "oxfmt": "0.68.0",
    "oxlint": "1.83.0",
    "tsx": "4.23.13",
    "turbo": "2.10.13",
    "typescript": "7.0.2"
  },
  "engines": {
    "node": ">=24.0.0 <25",
    "pnpm": "12.4.2"
  },
  "packageManager": "pnpm@12.4.2"
}
