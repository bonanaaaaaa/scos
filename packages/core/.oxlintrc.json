{
  "$schema": "../../node_modules/oxlint/configuration_schema.json",
  "rules": {
    "no-restricted-imports": [
      "error",
      {
        "paths": [
          {
            "name": "pg",
            "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
          },
          {
            "name": "hono",
            "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
          },
          {
            "name": "@scos/persistence",
            "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
          }
        ],
        "patterns": [
          {
            "regex": "^\\.\\.?/",
            "message": "Relative imports are not allowed: import through the package's \"imports\" map (\"#...\") or the workspace package name (\"@scos/core\"). See docs/architecture.md, \"Module specifiers\"."
          },
          {
            "group": ["@prisma/*", "hono/*", "@hono/*", "@scos/persistence/*"],
            "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
          }
        ]
      }
    ]
  },
  "overrides": [
    {
      "files": ["src/application/**"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "patterns": [
              {
                "regex": "^\\.\\.?/",
                "message": "Relative imports are not allowed: import through the package's \"imports\" map (\"#...\") or the workspace package name (\"@scos/core\"). See docs/architecture.md, \"Module specifiers\"."
              },
              {
                "group": [
                  "@prisma/*",
                  "prisma",
                  "pg",
                  "pg-*",
                  "hono",
                  "hono/*",
                  "@hono/*",
                  "@scos/persistence",
                  "@scos/persistence/*",
                  "@scos/api",
                  "@scos/api/*",
                  "node:*"
                ],
                "message": "Hexagonal layering: application/ must not import adapters or infrastructure (Prisma, pg, Hono, @scos/persistence, node:*); declare a port in application/ports instead."
              }
            ]
          }
        ]
      }
    },
    {
      "files": ["src/domain/**"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              {
                "name": "pg",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "name": "hono",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "name": "@scos/persistence",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              }
            ],
            "patterns": [
              {
                "regex": "^\\.\\.?/",
                "message": "Relative imports are not allowed: import through the package's \"imports\" map (\"#...\") or the workspace package name (\"@scos/core\"). See docs/architecture.md, \"Module specifiers\"."
              },
              {
                "group": ["@prisma/*", "hono/*", "@hono/*", "@scos/persistence/*"],
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "group": ["#application", "#application/**"],
                "message": "Hexagonal layering: domain must not depend on the application layer."
              }
            ]
          }
        ]
      }
    },
    {
      "files": ["src/domain/shared/**"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              {
                "name": "pg",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "name": "hono",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "name": "@scos/persistence",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              }
            ],
            "patterns": [
              {
                "regex": "^\\.\\.?/",
                "message": "Relative imports are not allowed: import through the package's \"imports\" map (\"#...\") or the workspace package name (\"@scos/core\"). See docs/architecture.md, \"Module specifiers\"."
              },
              {
                "group": ["@prisma/*", "hono/*", "@hono/*", "@scos/persistence/*"],
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "group": ["#application", "#application/**"],
                "message": "Hexagonal layering: domain must not depend on the application layer."
              },
              {
                "group": [
                  "#domain/pricing",
                  "#domain/pricing/**",
                  "#domain/shipping",
                  "#domain/shipping/**",
                  "#domain/ordering",
                  "#domain/ordering/**"
                ],
                "message": "Domain layering: shared/ must not import pricing/, shipping/, ordering/ (shared value objects depend on no other domain concept). Allowed order: shared <- pricing, shipping <- ordering."
              }
            ]
          }
        ]
      }
    },
    {
      "files": ["src/domain/pricing/**"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              {
                "name": "pg",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "name": "hono",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "name": "@scos/persistence",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              }
            ],
            "patterns": [
              {
                "regex": "^\\.\\.?/",
                "message": "Relative imports are not allowed: import through the package's \"imports\" map (\"#...\") or the workspace package name (\"@scos/core\"). See docs/architecture.md, \"Module specifiers\"."
              },
              {
                "group": ["@prisma/*", "hono/*", "@hono/*", "@scos/persistence/*"],
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "group": ["#application", "#application/**"],
                "message": "Hexagonal layering: domain must not depend on the application layer."
              },
              {
                "group": [
                  "#domain/shipping",
                  "#domain/shipping/**",
                  "#domain/ordering",
                  "#domain/ordering/**"
                ],
                "message": "Domain layering: pricing/ must not import shipping/, ordering/ (pricing is independent of shipping and is used by ordering). Allowed order: shared <- pricing, shipping <- ordering."
              }
            ]
          }
        ]
      }
    },
    {
      "files": ["src/domain/shipping/**"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "paths": [
              {
                "name": "pg",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "name": "hono",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "name": "@scos/persistence",
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              }
            ],
            "patterns": [
              {
                "regex": "^\\.\\.?/",
                "message": "Relative imports are not allowed: import through the package's \"imports\" map (\"#...\") or the workspace package name (\"@scos/core\"). See docs/architecture.md, \"Module specifiers\"."
              },
              {
                "group": ["@prisma/*", "hono/*", "@hono/*", "@scos/persistence/*"],
                "message": "Hexagonal layering: core must not import adapter technology (Prisma, pg, Hono) or the persistence adapter. Declare a port in application/ports and implement it in an adapter."
              },
              {
                "group": ["#application", "#application/**"],
                "message": "Hexagonal layering: domain must not depend on the application layer."
              },
              {
                "group": [
                  "#domain/pricing",
                  "#domain/pricing/**",
                  "#domain/ordering",
                  "#domain/ordering/**"
                ],
                "message": "Domain layering: shipping/ must not import pricing/, ordering/ (shipping is independent of pricing and is used by ordering). Allowed order: shared <- pricing, shipping <- ordering."
              }
            ]
          }
        ]
      }
    }
  ]
}
